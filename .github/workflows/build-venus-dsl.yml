name: Build Venus DSL Kernel (5.4.302)

# 第二阶段：原样复现 hushangda 的 DSL venus 内核（带 KSU/SuSFS/KPM）
# 不加 Docker，纯复现，验证自编内核也能开机。
# 第三阶段再加 Docker 配置。

on:
  workflow_dispatch:
    inputs:
      ENABLE_DOCKER:
        description: '是否注入 Docker 配置（第二阶段填 false，第三阶段填 true）'
        required: true
        default: 'false'
      ENABLE_KVM:
        description: '是否注入 KVM 硬件虚拟化配置（验证 888 能否跑 VM）'
        required: true
        default: 'false'
      UPDATE_KSU:
        description: '是否把KSU从旧ReSukiSU(30701)升级为SukiSU-Ultra上游(4字头,配新管理器)'
        required: true
        default: 'false'

env:
  ARCH: arm64
  CLANG_VERSION: clang-r416183b

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Free up disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL || true
          sudo docker image prune --all --force || true
          df -h

      - name: Set up build environment
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            git make bc bison flex libssl-dev libelf-dev \
            build-essential ca-certificates curl python3 \
            zip unzip lz4 cpio kmod

      - name: Checkout kernel source (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 1
          path: android-kernel

      - name: Show source info
        run: |
          cd android-kernel
          echo "=== kernel version (Makefile) ==="
          grep -E '^(VERSION|PATCHLEVEL|SUBLEVEL|EXTRAVERSION)' Makefile | head -4
          echo "=== KernelSU submodule ==="
          ls -la KernelSU || echo "KernelSU dir missing!"
          echo "=== venus_defconfig localversion ==="
          head -1 arch/arm64/configs/venus_defconfig

      - name: (Optional) Upgrade KSU to SukiSU-Ultra upstream
        if: ${{ github.event.inputs.UPDATE_KSU == 'true' }}
        run: |
          set -e
          cd android-kernel
          echo "=== 移除旧的 ReSukiSU 子模块(30701),换成 SukiSU-Ultra 上游 ==="
          # 旧的是 git submodule 形式,先彻底清掉,避免 setup.sh 与残留冲突
          git submodule deinit -f KernelSU 2>/dev/null || true
          rm -rf KernelSU .git/modules/KernelSU 2>/dev/null || true
          # 用官方 setup.sh --cleanup 清掉 drivers/Makefile|Kconfig 里的旧 kernelsu 引用(无论旧的怎么集成的)
          echo "=== 先 cleanup 旧的 drivers 引用 ==="
          curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s -- --cleanup || true
          # SukiSU-Ultra 官方集成: setup.sh 不带参数 = checkout 最新tag(对应4字头稳定版)
          # 注: 此脚本只装KSU本体, 不含SUSFS补丁(SUSFS仅用于隐藏root,与Docker/LXC无关,暂不集成)
          echo "=== 运行 SukiSU-Ultra setup.sh (最新tag) ==="
          curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s
          echo "=== 集成后 KernelSU 目录 + 当前tag ==="
          ls -la KernelSU 2>/dev/null | head || echo "KernelSU dir missing after setup!"
          ( cd KernelSU && git describe --tags 2>/dev/null && git log -1 --oneline 2>/dev/null ) || true
          echo "=== drivers 集成情况(符号链接/Makefile/Kconfig) ==="
          ls -la drivers/kernelsu 2>/dev/null; grep -n kernelsu drivers/Makefile 2>/dev/null; grep -n 'kernelsu/Kconfig' drivers/Kconfig 2>/dev/null

      - name: Download Android Clang toolchain (r416183b)
        run: |
          set -e
          CLANG_VER=clang-r416183b
          got=""

          # ---- 方式1: googlesource +archive (Actions 网络通常可达) ----
          echo "=== try1: googlesource archive ==="
          if curl -fL --retry 3 --connect-timeout 30 -o clang.tar.gz \
               "https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/main/${CLANG_VER}.tar.gz"; then
            if tar tzf clang.tar.gz >/dev/null 2>&1; then
              mkdir -p clang && tar xzf clang.tar.gz -C clang && got=1
              echo "got clang via googlesource archive"
            fi
          fi

          # ---- 方式2: git sparse-checkout 只拉 r416183b 子目录 ----
          if [ -z "$got" ]; then
            echo "=== try2: git sparse-checkout ==="
            rm -rf clang_repo
            git clone --depth=1 --filter=blob:none --sparse \
              https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86 clang_repo
            cd clang_repo
            git sparse-checkout set "${CLANG_VER}"
            cd ..
            if [ -d "clang_repo/${CLANG_VER}/bin" ]; then
              mv "clang_repo/${CLANG_VER}" clang && got=1
              echo "got clang via git sparse-checkout"
            fi
          fi

          # ---- 方式3: ZyCromerZ 镜像（最新ZyC clang，作为兜底；版本不同但能编5.4）----
          if [ -z "$got" ]; then
            echo "=== try3: ZyCromerZ ZyC clang fallback ==="
            ZURL=$(curl -fsSL "https://raw.githubusercontent.com/ZyCromerZ/Clang/main/Clang-main-link.txt" | tail -1)
            echo "ZyC url: $ZURL"
            mkdir -p clang
            curl -fL --retry 3 -o zyc.tar.gz "$ZURL"
            tar xzf zyc.tar.gz -C clang
            got=1
          fi

          if [ -z "$got" ]; then echo "ALL clang download methods failed"; exit 1; fi
          echo "=== clang version ==="
          ./clang/bin/clang --version | head -2

      - name: (Optional) Inject Docker configs into venus_defconfig
        if: ${{ github.event.inputs.ENABLE_DOCKER == 'true' }}
        run: |
          cd android-kernel
          DEFCONFIG=arch/arm64/configs/venus_defconfig
          echo "=== injecting Docker-required configs (最小集) ==="
          # stock/DSL内核实测缺失、docker硬性必需的项。先删旧行(避免 is-not-set 与新行冲突),再追加 =y
          for opt in PID_NS USER_NS IPC_NS POSIX_MQUEUE \
                     CGROUP_DEVICE CGROUP_PIDS \
                     BRIDGE_NETFILTER VLAN_8021Q \
                     NETFILTER_XT_MATCH_ADDRTYPE \
                     CHECKPOINT_RESTORE; do
            sed -i "/^# CONFIG_${opt} is not set/d" "$DEFCONFIG"
            sed -i "/^CONFIG_${opt}=/d" "$DEFCONFIG"
            echo "CONFIG_${opt}=y" >> "$DEFCONFIG"
          done
          echo "=== docker-related lines now ==="
          grep -E 'PID_NS|USER_NS|IPC_NS|CGROUP_DEVICE|CGROUP_PIDS|BRIDGE_NETFILTER|VLAN_8021Q|ADDRTYPE|POSIX_MQUEUE|CHECKPOINT' "$DEFCONFIG" || true

      - name: (Optional) Inject KVM virtualization configs into venus_defconfig
        if: ${{ github.event.inputs.ENABLE_KVM == 'true' }}
        run: |
          cd android-kernel
          DEFCONFIG=arch/arm64/configs/venus_defconfig
          echo "=== injecting ARM64 KVM configs ==="
          # ARM64 硬件虚拟化:总开关 VIRTUALIZATION + KVM,外加 vhost(virtio加速)
          for opt in VIRTUALIZATION KVM VHOST VHOST_NET VHOST_VSOCK \
                     VSOCKETS VIRTIO_VSOCKETS; do
            sed -i "/^# CONFIG_${opt} is not set/d" "$DEFCONFIG"
            sed -i "/^CONFIG_${opt}=/d" "$DEFCONFIG"
            echo "CONFIG_${opt}=y" >> "$DEFCONFIG"
          done
          echo "=== kvm-related lines now ==="
          grep -E 'VIRTUALIZATION|KVM|VHOST|VSOCK' "$DEFCONFIG" || true

      - name: Build kernel (replicate build_venus.sh)
        run: |
          cd android-kernel
          export PATH="$(pwd)/../clang/bin:$PATH"
          export ARCH=arm64
          export SUBARCH=arm64
          export LLVM=1
          export LLVM_IAS=1
          export CC=clang
          export LD=ld.lld
          export AR=llvm-ar
          export NM=llvm-nm
          export OBJCOPY=llvm-objcopy
          export OBJDUMP=llvm-objdump
          export STRIP=llvm-strip
          export READELF=llvm-readelf
          unset KSU_MANAGER_PACKAGE

          OUT=out
          mkdir -p "$OUT"

          MAKE_ARGS="O=$OUT ARCH=arm64 SUBARCH=arm64 LLVM=1 LLVM_IAS=1 CC=clang LD=ld.lld AR=llvm-ar NM=llvm-nm OBJCOPY=llvm-objcopy OBJDUMP=llvm-objdump STRIP=llvm-strip READELF=llvm-readelf"

          echo "=== make venus_defconfig ==="
          make $MAKE_ARGS venus_defconfig

          echo "=== inject ZRAM/F2FS/KSU/SuSFS/KPM via scripts/config (same as build_venus.sh) ==="
          ./scripts/config --file "$OUT/.config" \
            --enable ZRAM \
            --enable CRYPTO_LZ4 \
            --enable ZRAM_DEF_COMP_LZ4 \
            --disable ZRAM_DEF_COMP_LZORLE \
            --disable ZRAM_DEF_COMP_LZO \
            --disable ZRAM_DEF_COMP_ZSTD \
            --disable ZRAM_DEF_COMP_LZ4HC \
            --disable ZRAM_DEF_COMP_842 \
            --enable F2FS_FS_COMPRESSION \
            --enable F2FS_FS_LZO \
            --enable F2FS_FS_LZORLE \
            --enable F2FS_FS_LZ4 \
            --enable F2FS_FS_LZ4HC \
            --enable F2FS_FS_ZSTD \
            --enable F2FS_UNFAIR_RWSEM \
            --enable F2FS_CP_OPT \
            --enable KSU \
            --set-str KSU_FULL_NAME_FORMAT "%TAG_NAME%-%COMMIT_SHA%@%REPO_NAME%" \
            --enable KSU_MULTI_MANAGER_SUPPORT \
            --enable KSU_DISABLE_IN_RECOVERY \
            --disable KSU_TRACEPOINT_HOOK \
            --disable KSU_MANUAL_HOOK \
            --enable KSU_SUSFS \
            --enable KSU_SUSFS_SUS_PATH \
            --enable KSU_SUSFS_SUS_MOUNT \
            --enable KSU_SUSFS_SUS_KSTAT \
            --enable KSU_SUSFS_SPOOF_UNAME \
            --enable KSU_SUSFS_ENABLE_LOG \
            --enable KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS \
            --enable KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG \
            --enable KSU_SUSFS_OPEN_REDIRECT \
            --enable KSU_SUSFS_SUS_MAP \
            --enable KALLSYMS \
            --enable KALLSYMS_ALL \
            --enable BPF_STREAM_PARSER \
            --enable KPM

          echo "=== olddefconfig ==="
          make $MAKE_ARGS olddefconfig

          # KVM模式: 用 scripts/config 强制再开一遍(defconfig注入可能被 olddefconfig 依赖裁掉)
          if [ "${{ github.event.inputs.ENABLE_KVM }}" = "true" ]; then
            echo "=== force-enable KVM via scripts/config ==="
            ./scripts/config --file "$OUT/.config" \
              --enable VIRTUALIZATION \
              --enable KVM \
              --enable VHOST \
              --enable VHOST_NET \
              --enable VHOST_VSOCK \
              --enable VSOCKETS \
              --enable VIRTIO_VSOCKETS
            make $MAKE_ARGS olddefconfig
            echo "=== verify KVM configs took effect ==="
            for opt in VIRTUALIZATION KVM HAVE_KVM ARM64_VHE VHOST VHOST_NET; do
              if grep -q "^CONFIG_${opt}=[ym]" "$OUT/.config"; then
                echo "  OK  $(grep "^CONFIG_${opt}=" "$OUT/.config")"
              else
                echo "  !!  CONFIG_${opt} NOT enabled"
              fi
            done
          fi

          # Docker模式: 校验那几项是否真的生效(没生效就早停,别白等编译)
          if [ "${{ github.event.inputs.ENABLE_DOCKER }}" = "true" ]; then
            echo "=== verify docker configs took effect in .config ==="
            MISSING=""
            for opt in PID_NS USER_NS IPC_NS POSIX_MQUEUE \
                       CGROUP_DEVICE CGROUP_PIDS \
                       BRIDGE_NETFILTER VLAN_8021Q \
                       NETFILTER_XT_MATCH_ADDRTYPE CHECKPOINT_RESTORE; do
              if grep -q "^CONFIG_${opt}=y" "$OUT/.config"; then
                echo "  OK  CONFIG_${opt}=y"
              elif grep -q "^CONFIG_${opt}=m" "$OUT/.config"; then
                echo "  OK  CONFIG_${opt}=m (module)"
              else
                echo "  !!  CONFIG_${opt} NOT enabled"
                MISSING="$MISSING $opt"
              fi
            done
            if [ -n "$MISSING" ]; then
              echo "ERROR: 这些docker配置没生效(可能缺依赖):$MISSING"
              echo "继续编译但这些功能会缺失。检查依赖关系。"
            fi
          fi

          echo "=== build Image Image.gz dtbs ==="
          make -j"$(nproc)" $MAKE_ARGS Image Image.gz dtbs

          echo "=== built kernel version ==="
          if [ -f "$OUT/arch/arm64/boot/Image.gz" ]; then
            gzip -dc "$OUT/arch/arm64/boot/Image.gz" > /tmp/Image_check
            grep -a -m1 -o "Linux version 5[^)]*)" /tmp/Image_check || true
          fi
          ls -la "$OUT/arch/arm64/boot/"

      - name: Collect artifacts
        run: |
          cd android-kernel
          mkdir -p ../artifacts
          cp out/arch/arm64/boot/Image       ../artifacts/ 2>/dev/null || true
          cp out/arch/arm64/boot/Image.gz    ../artifacts/ 2>/dev/null || true
          cp out/arch/arm64/boot/dtb*        ../artifacts/ 2>/dev/null || true
          echo "=== artifacts ==="
          ls -la ../artifacts/

      - name: Upload kernel artifacts
        uses: actions/upload-artifact@v4
        with:
          name: venus-dsl-kernel-${{ github.event.inputs.ENABLE_DOCKER == 'true' && 'docker' || 'plain' }}${{ github.event.inputs.ENABLE_KVM == 'true' && '-kvm' || '' }}
          path: artifacts/*
